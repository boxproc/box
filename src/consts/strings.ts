export enum messagesConst {
  UNSAVED_CHANGES = 'You have unsaved changes.',
  CLOSE_MODAL_WINDOW = 'Close the modal window?',
}

export enum stringsConst {
  SHOW_LOG = 'Show log',
  DELETE = 'Delete',
  LOCK = 'Lock',
}
