name: main

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Create code archive
      - uses: actions/checkout@v2
        run: |
          tar cf - nginx ui | gzip > /tmp/box.tar.gz
          tar cf /tmp/box.tar nginx ui

      - name: Upload artifact
      - uses: actions/upload-artifact@v2
        if: success()
        with:
          name: new-version
          path: /tmp/box.tar*
